# HPU-GUIDER-AGENT
导游智能体前端，本项目使用vue框架，element-plus UI组件。代码内部兼容OPENAI式API，可以设置其他AI的API调用。

## 调试方法

电脑上首先要安装nodejs。打开vscode安装Vue插件，然后再powershell打开本地文件夹项目

`npm run dev`

或者直接点package.json的调试按钮。

## 发布版本

所有经过发布的版本都是基于electron构建的windows版本。
